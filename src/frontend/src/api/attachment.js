import client from './client'; export const uploadAttachment=(file,kind,taskId)=>{const f=new FormData();f.append('file',file);if(kind)f.append('kind',kind);if(taskId)f.append('taskId',taskId);return client.post('/attachments',f)};export const taskAttachments=id=>client.get(`/tasks/${id}/attachments`);export const deleteAttachment=id=>client.delete(`/attachments/${id}`)
